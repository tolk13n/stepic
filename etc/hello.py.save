CONFIG =   {
    'environment': {
        'PYTHONPATH': '/path/to/custom/python/packages',
    },

args: (
        --
bind = "0.0.0.0:8080",
        workers=4,
        # '--worker-class=egg:gunicorn#sync',
        # '--timeout=30',
  	daemon=True,
        'settings',
    ),
}

